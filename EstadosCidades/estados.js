var redline = require('readline')
var estadosCidades = require('./modulo/estados_cidades')

function getListaDeEstados(){
    const uf = []
    const quntidade = arrayEstados.length
    arrayEstados.array.forEach(function(a){
        uf.push(sigla)
    });
        const jsonEstado = {}
        jsonEstado.uf = uf
        jsonEstado.quantidade = quantidade

    return jsonEstado
    console.log(getListaDeEstados);
}

